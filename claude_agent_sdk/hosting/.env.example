# Copy to .env and fill in. .env is gitignored — never commit a real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the model. The hosting server defaults to claude-sonnet-4-6
# so your test runs stay cheap. Set this to match notebook 00's config:
# MODEL=claude-opus-4-6
